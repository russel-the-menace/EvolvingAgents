{
  "name": "evolving-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @evolving-agents/mindclone",
    "dev:mindclone": "npm run dev --workspace @evolving-agents/mindclone",
    "desktop": "npm run desktop --workspace @evolving-agents/mindclone",
    "test": "npm test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "sync:reference": "./scripts/sync-jd2resume-reference.sh"
  }
}
